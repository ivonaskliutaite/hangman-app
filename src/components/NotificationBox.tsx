import React from "react";
import styled from "styled-components";

const NotificationBox = styled.div``;

const NorificationBoxContainer = () => {
  return <NotificationBox />;
};

export default NorificationBoxContainer;
