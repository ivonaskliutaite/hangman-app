import React from "react";
import styled from "styled-components";

const WrongLettersContainer = styled.div``;
const WrongLettersDiv = styled.div``;

const WrongLetters = () => {
  return (
    <WrongLettersContainer>
      <WrongLettersDiv />
    </WrongLettersContainer>
  );
};

export default WrongLetters;
